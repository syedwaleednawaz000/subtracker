<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- LaunchTheme: Used during the app launch -->
    <style name="LaunchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- NormalTheme: Used for the main UI of the app -->
    <style name="NormalTheme" parent="Theme.MaterialComponents">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Alternative LaunchTheme: Used if DayNight mode is preferred -->
    <style name="DayNightLaunchTheme" parent="Theme.AppCompat.DayNight">
        <!-- You can add additional items here as needed -->
    </style>
</resources>
